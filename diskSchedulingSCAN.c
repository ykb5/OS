#include <stdio.h>
#include <stdlib.h>

void scan(int arr[], int n, int head)
{
    int seek = 0, dist, curr;
    
    return;
}
int main() {
    
    int n;
    printf("\nEnter the size: ");
    scanf("%d", &n);
    int arr[n], head;
    printf("\nEnter the elements: ");
    for(int i=0; i<n; i++) {
        scanf("%d", &arr[i]);
    }
    printf("\nEnter the head: ");
    scanf("%d", &head);
    scan(arr, n, head);

    return 0;
}